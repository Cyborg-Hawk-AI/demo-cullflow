import { DemoApp } from "@/components/demo/DemoApp";

export const metadata = {
  title: "Demo — CullFlow",
  description: "Interactive demo of CullFlow AI wedding photo culling and client gallery delivery.",
};

export default function DemoPage() {
  return <DemoApp />;
}
